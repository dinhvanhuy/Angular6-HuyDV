function candles(a, b){
    let result = a;
    let d, m;
    while(a>=b) {
         m = a;
         a = Math.floor(a/b);
         result += a;
         d  = m%b;
         a += d;
    }
    return result;
}
